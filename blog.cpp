#include <iostream>
#include <string.h>
#include <string>
using namespace std;
#define Add(X, Y)(X*Y)

// main() 함수 본문
int main()
{   
    int a = 30, b = 40;
    cout << "Add's result : " << Add(a, b) << endl; 
} 
